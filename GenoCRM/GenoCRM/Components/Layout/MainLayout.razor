@inherits LayoutComponentBase
@using System.Security.Claims
@using Microsoft.AspNetCore.Components.Authorization
@inject IStringLocalizer<GenoCRM.SharedResource> Localizer
<div class="page">
    <div class="sidebar">
        <NavMenu/>
    </div>

    <main>
        <div class="top-row px-4 d-flex justify-content-between align-items-center">
            <a href="https://learn.microsoft.com/aspnet/core/" target="_blank">@Localizer["About"]</a>
            <AuthorizeView>
                <Authorized>
                    <div class="d-flex align-items-center">
                        <span class="me-3">@Localizer["Welcome"], @context.User.FindFirst(ClaimTypes.GivenName)?.Value!</span>
                        <form method="post" action="/logout">
                            <button type="submit" class="btn btn-outline-secondary btn-sm">@Localizer["Logout"]</button>
                        </form>
                    </div>
                </Authorized>
                <NotAuthorized>
                    <a href="/login" class="btn btn-primary btn-sm">@Localizer["Login"]</a>
                </NotAuthorized>
            </AuthorizeView>
        </div>

        <article class="content px-4">
            @Body
        </article>
    </main>
</div>

<ModalContainer />

<div id="blazor-error-ui" data-nosnippet>
    @Localizer["UnhandledError"]
    <a href="." class="reload">@Localizer["Reload"]</a>
    <span class="dismiss">🗙</span>
</div>